import 'package:flutter/material.dart';

class FirstTry extends StatelessWidget {
  const FirstTry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Merhaba Antigravity!')));
  }
}
